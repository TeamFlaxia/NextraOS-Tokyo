---
# NextraOS Package Chooser Configuration
# Uses the built-in Calamares packagechooser module

mode: optionalmultiple
method: legacy

labels:
    step: "Ecosystems"
    step[ja]: "エコシステム"

items:
    - id: linux
      name: "Linux Apps (APT + Flatpak)"
      description: "Native Linux applications via APT packages and Flatpak. This is always installed."
      screenshot: ""

    - id: flatpak
      name: "Flatpak Support"
      description: "Add Flatpak application support with Flathub repository for access to thousands of additional apps."
      screenshot: ""

    - id: android
      name: "Android Apps (Waydroid)"
      description: "Run Android applications natively via Waydroid container. Requires installed system (not available in live mode)."
      screenshot: ""

    - id: windows
      name: "Windows Apps (QEMU/KVM)"
      description: "Run Windows applications in a virtual machine via QEMU/KVM. Additional ~32 GB storage recommended."
      screenshot: ""

    - id: macos
      name: "macOS Apps (Experimental)"
      description: "Run macOS applications in a virtual machine. Requires AVX2-capable CPU. Experimental status."
      screenshot: ""
